set nocompatible " turn off vi comptatibility turn on new features
      
set tabstop=4 " How many spaces <TAB> equals ?
set shiftwidth=4 " How many spaces autoindent use
set expandtab " replace <TAB> with spaces

set textwidth=79
set encoding=utf-8
set nu
set ruler " line numbering
set showmode

set bs=2 " backspace over EOL and autoindent, there is also start option
set scrolljump=5
set scrolloff=1

set ignorecase " search case insensitive by default
set noerrorbells " quiet

