set nocompatible " turn off vi comptatibility turn on some new features
set tabstop=8
set expandtab
set softtabstop=4 " How many spaces tab equals to
set shiftwidth=4 " How many spaces autoindent use
set textwidth=80 
set encoding=utf-8
set nu " line numbering
set bs=2 " backspace over EOL and something else
